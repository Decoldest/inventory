extends layout

block content
  h1= title

  div
    if book_list.length
      ul
        each book in book_list
          li
            a(href=book.url) #{book.name} #{book.price} Galleons

    else
      p There are no books.
  
  hr
  p
    a(href='book/create') Create Book