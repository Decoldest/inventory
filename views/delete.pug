extends layout 

block content 
  p Do you really want to delete this item - #{item.name}?

    form(method='POST')
      div.form-group
        input#itemid.form-control(type='hidden', name='itemid', value=item._id )

      button.btn.btn-primary(type='submit') Delete